package com.database.DAO;

import static org.junit.Assert.*;

import org.junit.Test;

import com.database.Model.Order;
import com.database.Service.OrderImplement;

public class orderDAOTest {

	/*
	 * @Test public void testAddOrder() { Order order= new Order();
	 * 
	 * order.setEmployee_ID(6); order.setOrder_Name("Laptop");
	 * order.setOrder_Quantity(2); order.setOrder_Price(60000);
	 * 
	 * OrderImplement imp=new OrderImplement(); assertEquals(true,
	 * imp.addOrder(order));
	 * 
	 * }
	 */
	
}
